---
title: "Bitácora automatizada"
---

En esta sección se puede observar la bitácora de los pushes mas recientes al repositorio en github. Todas las entradas en la siguiente tabla fueron generados por el sistema de documentación automática.

| Commit             | Owner       | Descripción del cambio | Fecha|
|--------------------|-------------|------------------------|------|
|dadbab5|Randall Sanchez|Cambios en: README.md|2025-08-07 18:49:29|
|dadbab5|Randall Sanchez|Se actualizó el archivo README.md para incluir insignias de estado de pruebas automatizadas con GitHub Actions,  versiones de Python, Django y MongoDB.  Se añadió una sección indicando la habilitación de pruebas automáticas en las ramas main/develop.  Mejora la documentación|2025-08-07 18:50:38|
|b249ba5|Randall Sanchez|Se corrigió la codificación en `auto_documentation.py` para asegurar la correcta lectura de la salida de `git diff` en Windows.  Se añadió `encoding='utf-8'` y `errors='replace'` al comando `subprocess.check_output` para manejar caracteres no UTF-8, previniendo errores de decodificación.|2025-08-07 18:52:35|
|b249ba5|Randall Sanchez|Se añadió `encoding='utf-8'` y `errors='replace'` al comando `subprocess.check_output` en `auto_documentation.py` para asegurar la correcta lectura de la salida de `git diff` en sistemas Windows, previniendo errores de codificación y reemplazando caracteres no UTF-8.|2025-08-07 18:54:38|
|86be4f8|Randall Sanchez|Se modificó `auto_documentation.py` para restringir el sistema al repositorio remoto 'personal'.  Se añadió un comentario indicando este cambio.  La configuración ahora apunta a un repositorio local específico (`WIKI_PATH`) en lugar de un repositorio remoto genérico.|2025-08-07 18:59:48|
|86be4f8|Randall Sanchez|Se modificó `auto_documentation.py` para usar exclusivamente un repositorio remoto 'personal'.  Se añadió un comentario indicando el cambio.  Esto simplifica la configuración del sistema, probablemente para desarrollo individual o pruebas.|2025-08-07 19:02:16|
|b243326|Randall Sanchez|Se añadió a `auto_documentation.py` la función `commit_and_push_wiki_changes`.  Esta función automatiza el proceso de  `quarto render`, `git add`, `git commit` y `git push` para actualizar el wiki tras generar la documentación.  Mejora la automatización del flujo de trabajo.|2025-08-07 19:08:44|
|a171b0e|Randall Sanchez|Se modificó el archivo `templates/base.html` para cambiar la paleta de colores de azul a verde.  Las variables CSS `--primary-blue`, `--light-blue`, `--lighter-blue`, `--lightest-blue`, y `--dark-blue` fueron reemplazadas por sus equivalentes en verde (`--primary-green`, etc.).|2025-08-07 20:47:58|
|2c59d9d|Randall Sanchez|Este commit mejora el sistema de generación automática de descripciones técnicas para cambios de código, específicamente abordando el problema de frases inconclusas.  El cambio se centra en la modificación de la solicitud (`prompt`) enviada al modelo de lenguaje Gemini.|2025-08-07 20:52:21|
|35db7a6|Randall Sanchez|Se revertían los cambios de color en la plantilla base `templates/base.html`.  Los colores verdes (`--primary-green`, etc.) fueron reemplazados por los azules originales (`--primary-blue`, etc.), revirtiendo una modificación previa.  Esto restaura la apariencia visual inicial de la plantilla.|2025-08-07 20:53:53|
|846480b|Randall Sanchez|Se eliminaron emojis (✅, ❌) de los mensajes de consola en `psybot/settings.py` referentes a la conexión MongoDB.  Esto mejora la portabilidad del código, evitando problemas con consolas que no soportan emojis y simplificando la salida.  Se reemplazó  "✅ Conectado..." por "Conectado...|2025-08-07 21:01:17|